<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Roblox Engine — Ultra</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
:root{
  --bg:#05070f;
  --panel:#0d1433;
  --card:#121a3d;
  --neon:#00fff0;
  --accent:#ff3b3b;
  --text:#eaeaea;
}

body{
  margin:0;
  background:radial-gradient(circle at top,#10184a,#05070f);
  color:var(--text);
  font-family:Segoe UI, system-ui;
}

.app{
  display:flex;
  height:100vh;
}

/* SIDEBAR */
.sidebar{
  width:240px;
  background:linear-gradient(180deg,#0b1230,#05070f);
  border-right:1px solid #1f2a4a;
  padding:20px;
}

.sidebar h2{
  color:var(--neon);
  text-shadow:0 0 15px var(--neon);
}

.sidebar a{
  display:block;
  padding:12px;
  margin-top:10px;
  color:#cfd6ff;
  text-decoration:none;
  border-radius:10px;
}

.sidebar a:hover{
  background:#121a3d;
  box-shadow:0 0 10px #00fff055;
}

/* MAIN */
.main{
  flex:1;
  padding:30px;
  overflow:auto;
}

.page{display:none}
.page.active{display:block}

/* CARDS */
.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(250px,1fr));
  gap:20px;
}

.card{
  background:linear-gradient(145deg,var(--card),#0b1028);
  padding:20px;
  border-radius:18px;
  border:1px solid #1f2a4a;
  transition:.3s;
}

.card:hover{
  transform:translateY(-6px);
  box-shadow:0 0 25px #00fff066;
}

.card h3{color:var(--neon)}

/* AI */
.chat{
  height:300px;
  background:#040716;
  border-radius:15px;
  padding:15px;
  overflow-y:auto;
}

.msg{margin-bottom:10px}
.user{color:#ff7b7b}
.bot{color:#7cffea}

.input{
  display:flex;
  gap:10px;
  margin-top:10px;
}

.input input{
  flex:1;
  padding:12px;
  border-radius:10px;
  border:none;
  background:var(--panel);
  color:white;
}

/* LOGIN */
.modal{
  position:fixed;
  inset:0;
  background:#000a;
  display:flex;
  align-items:center;
  justify-content:center;
}

.modal-box{
  background:var(--panel);
  padding:30px;
  border-radius:20px;
  width:300px;
  text-align:center;
}

button{
  padding:10px 20px;
  border:none;
  border-radius:10px;
  background:var(--accent);
  color:white;
  cursor:pointer;
}
</style>
</head>

<body>

<div id="loginModal" class="modal">
  <div class="modal-box">
    <h2>Login</h2>
    <input id="usernameInput" placeholder="Username" style="width:100%;padding:10px;border-radius:10px;border:none;">
    <br><br>
    <button onclick="login()">Enter Roblox Engine</button>
  </div>
</div>

<div class="app">
  <div class="sidebar">
    <h2>ROBLOX</h2>
    <a onclick="showPage('dashboard')">🏠 Dashboard</a>
    <a onclick="showPage('games')">🎮 Games</a>
    <a onclick="showPage('dev')">🧑‍💻 Dev Hub</a>
    <a onclick="showPage('ai')">🤖 AI</a>
    <a onclick="showPage('settings')">⚙️ Settings</a>
    <p id="userLabel" style="margin-top:20px;opacity:.7"></p>
  </div>

  <div class="main">

    <div id="dashboard" class="page active">
      <h1>Welcome to Roblox Engine</h1>
      <div class="grid">
        <div class="card"><h3>65M+</h3><p>Online Players</p></div>
        <div class="card"><h3>40M+</h3><p>Games</p></div>
        <div class="card"><h3>AI</h3><p>Status: ONLINE</p></div>
      </div>
    </div>

    <div id="games" class="page">
      <h1>Trending Games</h1>
      <div class="grid">
        <div class="card"><h3>Blox Fruits</h3></div>
        <div class="card"><h3>Adopt Me</h3></div>
        <div class="card"><h3>Arsenal</h3></div>
      </div>
    </div>

    <div id="dev" class="page">
      <h1>Developer Hub</h1>
      <div class="grid">
        <div class="card"><h3>Scripts</h3><p>Combat, UI, DataStore</p></div>
        <div class="card"><h3>Analytics</h3><p>Retention & Monetization</p></div>
      </div>
    </div>

    <div id="ai" class="page">
      <h1>Roblox AI</h1>
      <div class="chat" id="chat">
        <div class="msg bot">AI: Ultra AI Online ⚡</div>
      </div>
      <div class="input">
        <input id="chatInput" placeholder="Ask the AI..." />
        <button onclick="askAI()">Send</button>
      </div>
    </div>

    <div id="settings" class="page">
      <h1>Settings</h1>
      <button onclick="setTheme('blue')">Neon Blue</button>
      <button onclick="setTheme('red')">Neon Red</button>
    </div>

  </div>
</div>

<script>
let user = localStorage.getItem("user");

if(user){
  document.getElementById("loginModal").style.display="none";
  document.getElementById("userLabel").innerText="User: "+user;
}

function login(){
  const u=document.getElementById("usernameInput").value;
  if(!u)return;
  localStorage.setItem("user",u);
  location.reload();
}

function showPage(id){
  document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));
  document.getElementById(id).classList.add("active");
}

function askAI(){
  const i=document.getElementById("chatInput");
  const c=document.getElementById("chat");
  const q=i.value.trim();
  if(!q)return;
  c.innerHTML+=`<div class="msg user">You: ${q}</div>`;
  let a="Roblox AI processing...";
  if(q.includes("robux")) a="Earn Robux with passes, UGC & premium payouts.";
  if(q.includes("/dev")) a="Learn Luau, focus on retention systems.";
  setTimeout(()=>c.innerHTML+=`<div class="msg bot">AI: ${a}</div>`,600);
  i.value="";
}

function setTheme(t){
  if(t==="red") document.documentElement.style.setProperty("--neon","#ff3b3b");
  else document.documentElement.style.setProperty("--neon","#00fff0");
}
</script>

</body>
</html>
